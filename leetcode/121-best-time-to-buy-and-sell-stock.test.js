const maxProfit = (prices) => {};

describe('maxProfit', () => {
  it('case 1', () => {
    expect(maxProfit([6, 5, 3, 1])).toBe(0);
  });

  it('case 2', () => {
    expect(maxProfit([7, 1, 5, 3, 6, 4])).toBe(5);
  });
});
